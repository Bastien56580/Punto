/*const mongoose = require('mongoose')

const boardSchema = new mongoose.Schema({
    panel:[{
        panel:{type : Array, default:Array(11)
                .fill(null)
                .map(() => Array(11)
                    .fill(null))
        } ,
        required: true
    }]
})
module.exports = mongoose.model('board', boardSchema)
 */